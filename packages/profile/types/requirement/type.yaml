# Type module of requirement; labels live in messages/, the note template in template.md.
# Validated by packages/core/schemas/type-module.schema.json.
group: motivation
status: planned
glyph: requirement
attributes:
  priority: { type: string }
  origin: { type: string }
