# Type module of constraint; labels live in messages/, the note template in template.md.
# Validated by packages/core/schemas/type-module.schema.json.
group: motivation
status: planned
glyph: constraint
attributes:
  origin: { type: string }
