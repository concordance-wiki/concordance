# Type module of actor; labels live in messages/, the note template in template.md.
# Validated by packages/core/schemas/type-module.schema.json.
group: business
status: planned
glyph: actor
attributes:
  external: { type: boolean }
