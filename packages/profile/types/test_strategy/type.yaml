# Type module of test_strategy; labels live in messages/, the note template in template.md.
# Validated by packages/core/schemas/type-module.schema.json.
group: quality
status: planned
glyph: strategy
attributes:
  applies_to: { type: "ref[]", target: any }
  levels: { type: "string[]" }
