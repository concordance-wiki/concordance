# Type module of gap; labels live in messages/, the note template in template.md.
# Validated by packages/core/schemas/type-module.schema.json.
group: implementation
status: planned
glyph: gap
attributes:
  from: { type: ref, target: plateau }
  to: { type: ref, target: plateau }
