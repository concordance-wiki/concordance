# Type module of plateau; labels live in messages/, the note template in template.md.
# Validated by packages/core/schemas/type-module.schema.json.
group: implementation
status: planned
glyph: plateau
attributes:
  date: { type: date }
  applications: { type: "ref[]", target: application }
