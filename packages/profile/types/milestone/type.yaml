# Type module of milestone; labels live in messages/, the note template in template.md.
# Validated by packages/core/schemas/type-module.schema.json.
group: implementation
status: planned
glyph: milestone
attributes:
  date: { type: date }
