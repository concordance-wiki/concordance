# Changelog

Managed by Changesets: every package keeps its changelog next to it (`packages/*/CHANGELOG.md`, `plugins/*/CHANGELOG.md`, `presets/*/CHANGELOG.md`), written by the version pull request, and the GitHub release of every version gathers the entries of the version once. See [the release guide](docs/guides/releasing.md).
