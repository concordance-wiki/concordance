# presets

The [`@concordance-wiki/concordance`](concordance/README.md) package. It depends on the command line and on every official plugin, exposes the `concordance` and `conc` commands, and is what the documentation tells integrators to install. The container image is built from it.
