# presets

The unscoped `concordance` package. It depends on every core package and every official plugin, exposes the `concordance` and `conc` commands, and ships a configuration that enables all plugins. It is what the documentation tells integrators to install.

Empty until the core and the plugins exist.
