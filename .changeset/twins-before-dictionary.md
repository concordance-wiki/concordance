---
"@concordance-wiki/cli": patch
---

The twin resources are reconciled before the recognition dictionary is built: a document folded into its note no longer enters the dictionary as a second entity of the same title, so the shared title yields no `I-TERM-HOMONYM`, its occurrences are read at full confidence and the co-occurrence rows of the folded twin count on the merged entity.
