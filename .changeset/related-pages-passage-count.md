---
"@concordance-wiki/site": minor
"@concordance-wiki/i18n": minor
---

Related pages: the entries are ordered by number of passages, written links and recognised mentions counted alike, a written link marking its entry "Cited" and quoting the sentence around it; six entries stand in view before the button naming the others, three where the panel is condensed, two on a phone, the served pages beyond six behind a disclosure worded as the button so that a reader without JavaScript reaches them; a keyword page, a meeting and a document page, which nothing links to, list the pages their own passages evoke, from the passages of the keyword fragment or the provenances read in the files of the page, under the note of their kind (`document.relatedNote` added); the served mentions start with the first passage of each listed page, every passage of a page saying how many the page holds (`passages`), so that a slice counts right; `related.orderNote` says the rule and the `leadNote` label goes, the API page giving its note as the order note.
