---
"@concordance-wiki/cli": minor
---

`concordance query` narrows the links of an entity with `--direction in|out` and `--relation <slug>`, lists what lies within `--radius` links with `--near`, and explains why two entities are linked with `--explain <target>`, every provenance of every link between them shown with its place and context.
