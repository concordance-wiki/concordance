---
"@concordance-wiki/cli": patch
---

`concordance --help` names `--fix` and `--dry-run` among the options of `lint`, and the command-line guide carries the same usage text, checked by the validation.
