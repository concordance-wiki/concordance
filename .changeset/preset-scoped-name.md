---
"@concordance-wiki/concordance": minor
---

The preset is published as `@concordance-wiki/concordance`: the unscoped name belongs to another package on npm. Install it with `npm install --global @concordance-wiki/concordance`; the `concordance` command and the container image keep their names.
