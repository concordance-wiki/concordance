---
"@concordance-wiki/cli": minor
---

`concordance query` answers the questions of the whole corpus: `--stats`, `--sources`, `--domains`, `--undefined [<expression>]` with `--min-files`, `--recent` with `--since` and `--source`, `<expression> --changed-with`, and `--findings` about an entity or under `--check`.
