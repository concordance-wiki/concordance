---
"@concordance-wiki/site": patch
---

The mentions of an entity's own page are computed once per build and shared by its panel, its fragment and the count of its citing pages, instead of three times; a pivot entity cited by thousands of passages no longer pays that work three times over.
