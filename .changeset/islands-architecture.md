---
"@concordance-wiki/site": minor
---

Add the rendering foundation of the site: eleven named slots with typed view models, a default theme of Preact components rendered to static HTML, islands hydrated only where a page needs them with one esbuild bundle each, theme overrides resolved from the `theme` contributions of plugins, the stylesheet in four cascade layers generated from `theme.yaml`, and the page and bundle budget for the build summary.
