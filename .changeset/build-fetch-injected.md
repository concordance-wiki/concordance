---
"@concordance-wiki/cli": patch
---

`concordance build` reaches the network through the `fetch` of the command io alone, as `lint` does: a caller that gives none forbids every connection; the gallery reads the type modules of the core through the injected file system.
