---
"@concordance-wiki/core": patch
---

A speaker is looked up in the pseudonym dictionary through an index of the names by their key, built once per dictionary and locale, instead of segmenting every name of the dictionary at every cue; the speakers a transcript numbers are keyed by the locale of the transcript, as the dictionary lookup is, so that one person never takes two numbers.
