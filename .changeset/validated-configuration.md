---
"@concordance-wiki/core": minor
"@concordance-wiki/cli": minor
---

Validate the configuration before any processing: `concordance validate-config` reports every problem with its path, the value received and the values expected; `concordance init` writes a minimal valid configuration; `concordance build` stops on an invalid configuration.
