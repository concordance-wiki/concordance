---
"@concordance-wiki/site": minor
---

To-do page: two lists and nothing else, the documents without a markdown representation (the entities the `W-DOC-NOMD` findings name, with their number of files) and the words above the threshold without a note (the keyword pages, with their occurrences and, new, the files they spread over as `files` on the entry), each entry leading to the page concerned, most first then by identifier; every other finding is ignored, and a test holds it against broken links, stale sources and ambiguous relations; the header of every page and the home page link to it with the total of both lists.
