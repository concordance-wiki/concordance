---
"@concordance-wiki/site": patch
---

The islands read the `hidden` flag of an element as the platform now types it (a boolean or the `until-found` keyword), so that the site builds under TypeScript 6; nothing changes in a page.
