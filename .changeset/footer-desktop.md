---
"@concordance-wiki/site": patch
---

The page fills the viewport and its columns reach the footer: the tree and the panel of an entity page no longer stop above a band of ground, and the footer card spans the page as the reference design draws it instead of floating narrower than the columns.
