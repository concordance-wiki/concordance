---
"@concordance-wiki/core": patch
---

A symbolic link inside a source is never followed, whether it points at a file or a folder: a repository could otherwise make the build publish any file it can read, the pseudonym dictionary first.
