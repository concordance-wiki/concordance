---
"@concordance-wiki/plugin-reader-office": patch
---

The reader-office plugin declares its plugin API version through the `PLUGIN_API_VERSION` constant of the core, as the other official plugins do, so a future bump of the API version reaches it without a literal to update.
