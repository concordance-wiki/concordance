---
"@concordance-wiki/core": patch
"@concordance-wiki/ingest": patch
---

Git never waits for anyone: Git Credential Manager is told not to ask, ssh runs in batch mode unless the caller names its own command, and a command that stalls is stopped after fifteen minutes, or the seconds `CONCORDANCE_GIT_TIMEOUT` gives; a source behind an unknown host key is reported without credentials.
