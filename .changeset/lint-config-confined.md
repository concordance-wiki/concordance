---
"@concordance-wiki/lint": patch
---

`concordance-lint.yaml` travels with a pull request, from a fork too: `global.cache_dir` and `global.profile` must stay inside the repository, and a `global.model` URL pointing at the runner, its link-local neighbours or a private network is refused; the global scope degrades with a reason naming the key.
