---
"@concordance-wiki/lint": minor
"@concordance-wiki/inference": minor
---

Linter and build parity: `LOCAL_CHECKS` names the checks the local scope computes, the linter reports a broken link with the entity and the wording of the build, the explicit links step takes its remediation from the catalogue, `concordance lint --format json` carries `scope` and `checks`, and a parity test compares the findings of the linter and of the build on every fixture corpus.
