---
"@concordance-wiki/profile": minor
"@concordance-wiki/core": minor
---

Load the embedded default profile, merge a project profile on top of it key by key (arrays replaced, `allowed` pairs added), validate the result against the schema and its declared slugs, fingerprint it, and answer the allowed relation matrix for a pair of types; `describeSchemaError` becomes public in core.
