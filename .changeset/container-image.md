---
"concordance": minor
"@concordance-wiki/cli": minor
---

Add the `concordance` preset package, the container image built from it and the pipeline that verifies the image; the command line exposes its executable as `@concordance-wiki/cli/bin`.
