---
"@concordance-wiki/site": minor
---

The component gallery samples (`galleryFixtures`) describe Concordance itself: an explicit link term, the mentions panel, the model query API and the `build summary` keyword page replace the former insurance examples.
