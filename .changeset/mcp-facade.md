---
"@concordance-wiki/cli": minor
---

`concordance mcp` serves the questions of `query` to an agent over the standard input as tools of the model context protocol, one tool per family (`lookup`, `search`, `relations`, `list`, `corpus`, `passages`), without state, key or network; `distribution/mcp/mcp.json` is the configuration a harness reads to start it.
