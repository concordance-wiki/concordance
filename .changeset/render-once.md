---
"@concordance-wiki/site": patch
---

A page is rendered once, the shell around its content, instead of twice to learn which islands it uses; the accessibility checker walks the tree of a page once per rule instead of once per element. The site is the same byte for byte and builds in a third less time on the golden corpus.
