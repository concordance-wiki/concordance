---
aliases: [référence typée]
broader: lien
---
# Référence de frontmatter

Un identifiant écrit dans un attribut typé du frontmatter tel que `roles`, `reads`, `writes`, `broader` ou `depends_on`. Le profil nomme la relation ; la confiance est de 0,9.
