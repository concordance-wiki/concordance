---
aliases: [cache]
---
# Fingerprint cache

The store of converted documents and extracted contracts, keyed by the hash of their bytes, so that a build converts a file once. Pruned of unused entries by the cache pruning batch.
