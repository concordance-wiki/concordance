---
aliases: [lint]
---
# Linter

The command a forge runs on a merge request: it builds the changed files, applies every check and fails under the fail-on policy. Its findings are posted as a forge report.
