---
aliases: [threshold]
---
# Publication threshold

The minimum a candidate expression must reach to get a keyword page: three occurrences across two files by default. Applied once, when the canonical model is written.
