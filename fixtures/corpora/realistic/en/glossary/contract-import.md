---
aliases: [contract extraction]
---
# Contract import

The reading of an OpenAPI or WSDL contract declared by an API note: every operation becomes an endpoint linked to the API at 0.95, matched to an existing endpoint note on its operation identifier.
