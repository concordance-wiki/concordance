---
aliases: [model]
---
# Canonical model

The single `model.json` file a build writes: entities, links, findings and keyword pages, sorted canonically so that two builds of the same corpus produce the same bytes. Everything rendered comes from it.
