---
aliases: [typed reference]
broader: link
---
# Frontmatter reference

An identifier written in a typed frontmatter attribute such as `roles`, `reads`, `writes`, `broader` or `depends_on`. The profile names the relation; the confidence is 0.9.
