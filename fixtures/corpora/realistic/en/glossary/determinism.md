---
aliases: [reproducible build]
---
# Determinism

The property that two builds of the same corpus with the same configuration produce the same canonical model and the same pages, byte for byte. Every list is sorted and the clock is never read.
