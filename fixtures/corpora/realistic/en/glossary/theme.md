---
aliases: [site theme]
---
# Theme

The set of templates, styles and fonts that renders the canonical model into pages. The default theme ships with the tool; a theme author overrides its slots one by one.
