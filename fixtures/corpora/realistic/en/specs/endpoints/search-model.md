---
method: GET
path: /search
api: api/model-query
operation_id: searchModel
---
# Search the model

Returns the entities and keyword pages whose title, alias or identifier matches the query, best score first. The same ranking as the search index of the static site.

## Consumers

- [Pinned trail](../screens/service/pinned-trail.md)
