---
lifecycle: [started, validated, rendered, published, failed]
---
# Build

One run of the pipeline, from the validation of the configuration to the publication of the site, with its build log and its [findings](finding.md). A build that fails under the fail-on policy publishes nothing.
