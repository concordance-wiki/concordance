---
lifecycle: [listed, read, converted, excluded]
---
# Resource

A file of a [source](source.md): a markdown note, a document to convert, a contract or an image. A resource under `privacy.exclude` is never read; a converted one gets a [representation](representation.md).
