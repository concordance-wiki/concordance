---
lifecycle: [inferred, promoted, published]
---
# Link

An edge between two [entities](entity.md), with a relation, a confidence and one provenance per method that found it. Promoted to 1.0 by the lock file, capped at 0.6 when the relation falls back to related. Stored in the [LINKS table](../tables/links.table.md).
