# Faulty corpus (French)

Same structure and same expected findings as the English corpus, with French file names and content, so that the `fr` language pack is tested on the same cases. See `../en/README.md` for what is not covered here.
